#include <bits/stdc++.h>
using namespace std;

class Solution{
  public:
    long long int countSpltInversions(long long X[],long long Y[],long long m, long long n){

    }
    long long int inversionCount(long long arr[], long long N){
        if(N==1){
            return 0;
        }
        int mid=floor(N/2);
        long long int X[]; 
        for(int i=0;i<=mid;i++){
            X[i]=arr[i];
        }
        a1=inversionCount(arr,m);
        a1=inversionCount(arr,m);
    }

};